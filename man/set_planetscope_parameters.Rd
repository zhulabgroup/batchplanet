% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_planetscope_parameters.R
\name{set_data_directory}
\alias{set_data_directory}
\title{Prompt for and Store Data Directory Path}
\usage{
set_data_directory(change_directory = FALSE)
}
\arguments{
  \item{change_directory}{Logical. If \code{TRUE}, forces re-prompting for a new directory path even if one already exists (default: \code{FALSE}).}
}
\description{
Loads (or creates) a \code{.env} file, reads the \code{data_directory}
environment variable, and if it is missing (or if \code{change_directory = TRUE}),
prompts the user to enter a path. The provided path is normalized and saved to
\code{.env} for future sessions.
}
\value{
Character. The absolute path to the data directory.
}
\details{
\itemize{
  \item Creates \code{.env} if it doesn’t exist.
  \item Uses \code{dotenv::load_dot_env()} to load existing variables.
  \item Saves \code{data_directory=<normalized_path>} to \code{.env}.
}
}
\examples{
\dontrun{
# First run: prompts for path, then saves to .env
dir <- set_data_directory()

# To change the saved directory:
new_dir <- set_data_directory(change_directory = TRUE)
}
}
