% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_time_series.R
\name{visualize_time_series}
\alias{visualize_time_series}
\title{Visualize time series interactively}
\usage{
visualize_time_series(
  df_ts,
  df_doy = NULL,
  var = "value",
  ylab = "Value",
  smooth = FALSE,
  lambda = 50,
  facet_var = NULL,
  color_palette = "viridis"
)
}
\arguments{
\item{df_ts}{Data frame. Must contain either a \code{time} (POSIX) or \code{date} (Date) column, an \code{id} column, and the variable to plot.}

\item{df_doy}{Optional data frame. Time of phenological events with columns \code{year} and \code{doy}. Default: \code{NULL}.}

\item{var}{Character. Name of the column in \code{df_ts} to plot (default: \code{"value"}).}

\item{ylab}{Character. Label for the y-axis (default: \code{"Value"}).}

\item{smooth}{Logical. If \code{TRUE}, applies gap-filling and smoothing to data points with Whittaker smoothing (default: \code{FALSE}).}

\item{lambda}{Numeric. Smoothing parameter for Whittaker smoothing (default: 50).}

\item{facet_var}{Character or \code{NULL}. Column name in \code{df_ts} and \code{df_doy} to facet by (e.g., \code{"site"} or \code{"id"}).}

\item{color_palette}{Character. Name of a viridis palette for line colors (default: \code{"viridis"}).}
}
\value{
An interactive plotly object.
}
\description{
Generates an interactive plotly time series plot from a data frame, optionally overlaying phenological events.
}
\examples{
\dontrun{
# Example: Visualize a time series with phenological events
visualize_time_series(
  df_ts = df_ts,
  df_doy = df_doy,
  var = "value",
  ylab = "Value",
  smooth = TRUE,
  lambda = 50,
  facet_var = "id",
  color_palette = "viridis"
)
}

}
