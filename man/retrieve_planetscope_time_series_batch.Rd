% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve_planetscope_time_series.R
\name{retrieve_planetscope_time_series_batch}
\alias{retrieve_planetscope_time_series_batch}
\title{Retrieve PlanetScope time series for multiple sites and groups}
\usage{
retrieve_planetscope_time_series_batch(
  dir,
  df_coordinates,
  v_site = NULL,
  v_group = NULL,
  max_sample = 2000,
  num_cores = 12
)
}
\arguments{
\item{dir}{Character. Base directory containing raw PlanetScope data (expects a \verb{raw/} subdirectory).}

\item{df_coordinates}{Data frame with columns \code{site}, \code{lon}, \code{lat}, \code{id}, and optionally \code{group}.}

\item{v_site}{Character vector, optional. Site identifiers to process; if \code{NULL}, all sites in \code{df_coordinates} are included.}

\item{v_group}{Character vector, optional. Group identifiers to process; if \code{NULL}, all groups in \code{df_coordinates} are included.}

\item{max_sample}{Integer. Maximum number of samples per group (default: 2000). This is to avoid processing too many points at once, which can be slow.}

\item{num_cores}{Integer. Number of parallel workers for processing (default: 12).}
}
\value{
Invisibly returns \code{NULL} and saves one \code{.rds} file per site-group to the \verb{ts/} subdirectory in the specified \code{dir}.
}
\description{
Extracts time series data from PlanetScope imagery for all points in a coordinate data frame, grouped by site and group.
For each site and group, reflectance, QA, and metadata are extracted and combined
Results are saved as \code{.rds} files under the \verb{ts/} subdirectory of the specified \code{dir}, prefixed \code{ts_}.
}
\examples{
\dontrun{
retrieve_planetscope_time_series_batch(
  dir = "alldata/PSdata/",
  df_coordinates = df_coordinates,
  v_site = c("HARV", "SJER"),
  v_group = c("Acer", "Quercus"),
  max_sample = 2000,
  num_cores = 12
)
}

}
