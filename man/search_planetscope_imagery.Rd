% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_planetscope_imagery.R
\name{search_planetscope_imagery}
\alias{search_planetscope_imagery}
\title{Search for available PlanetScope imagery}
\usage{
search_planetscope_imagery(
  api_key,
  bbox,
  date_end = NULL,
  date_start = NULL,
  item_name = "PSScene",
  asset = "ortho_analytic_4b_sr",
  cloud_lim = 0.1,
  ground_control = TRUE,
  quality = "standard"
)
}
\arguments{
\item{api_key}{Character. Your Planet API key.}

\item{bbox}{Named numeric list with \code{xmin}, \code{ymin}, \code{xmax}, \code{ymax} defining the search area.}

\item{date_end}{Character. End date ("YYYY-MM-DD") for the search (inclusive).}

\item{date_start}{Character. Start date ("YYYY-MM-DD") for the search (inclusive).}

\item{item_name}{Character. Planet item type to search (default: \code{"PSScene"}).}

\item{asset}{Character. Asset type to filter permissions (default: \code{"ortho_analytic_4b_sr"}).}

\item{cloud_lim}{Numeric. Maximum allowed cloud cover fraction (0–1, default: 0.1).}

\item{ground_control}{Logical. If \code{TRUE}, require ground control metadata (default: \code{TRUE}).}

\item{quality}{Character. Quality category filter (default: \code{"standard"}).}
}
\value{
Character vector of image IDs with download permission. Returns \code{NULL} if none are found.
}
\description{
Queries the Planet API for imagery overlapping the specified bounding box and date range, then filters results by cloud cover, ground control, and quality.
Returns only IDs for which you have download permission.
}
\examples{
\dontrun{
my_bbox <- list(xmin = -77.05, ymin = 38.80, xmax = -76.90, ymax = 39.00)
ids <- search_planetscope_imagery(
  api_key = set_api_key(),
  bbox = my_bbox,
  date_start = "2023-06-01",
  date_end = "2023-06-30",
  cloud_lim = 1,
  ground_control = TRUE,
  quality = "standard"
)
}

}
