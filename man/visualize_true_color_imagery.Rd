% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_true_color_imagery.R
\name{visualize_true_color_imagery}
\alias{visualize_true_color_imagery}
\title{Visualize true-color raster imagery}
\usage{
visualize_true_color_imagery(
  file,
  df_coordinates = NULL,
  brightness = 5,
  global_brightness = 0.05
)
}
\arguments{
\item{file}{Character. Path to a multi-band raster file (e.g., PlanetScope SR clip).}

\item{df_coordinates}{Optional data frame. Point locations to overlay; must contain \code{lon}, \code{lat}, and \code{site}. Default: \code{NULL}.}

\item{brightness}{Numeric. Brightness multiplier for RGB values (slider value, default: 5).}

\item{global_brightness}{Numeric. The global average brightness (from sampled images at app startup).}
}
\value{
A ggplot2 object.
}
\description{
Reads a multi-band raster, converts the red, green, and blue bands to normalized RGB values, and creates a ggplot2 tile plot of the true-color composite. Optionally overlays point locations. Automatically normalizes image brightness to the global average (computed at app startup) for legibility, then applies the user brightness slider (0-10, default 5) as a multiplier.
}
\examples{
\dontrun{
visualize_true_color_imagery(
  file = "alldata/PSdata/raw/SJER/20240501_SR_harmonized_clip.tif",
  df_coordinates = df_coordinates_SJER,
  brightness = 5,
  global_brightness = 0.05
)
}

}
